# Legacy file-based ledger demo

Standalone Node demo (JSON file ledger). **Not used by the Next.js app.**

The production general ledger lives in Supabase via `lib/ledger.ts`.

```bash
npx tsx examples/ledger-demo/index.ts
```
